function Header(){
    
    //This component renders the header at the top of the page. 
    return (
        <div className="header--container">
            <h1 className="header--title">Abstract Wallet Generator App</h1>
        </div>
    )
}

export default Header